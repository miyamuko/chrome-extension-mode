package: chrome-extension-mode
version: 0.0.1

sources:
  include:
    - site-lisp/chrome-extension-mode/**/*.l
  exclude:
    - site-lisp/chrome-extension-mode/**/tests/*.l

document:
  - README.rdoc

files:
  include:
    - site-lisp/**/*.l
    - site-lisp/**/*.lc
    - site-lisp/chrome-extension-mode/examples/**/*.*
    - site-lisp/chrome-extension-mode/tests/*.*
